<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120" width="120" height="120">
  <!-- Circular green background -->
  <circle cx="60" cy="60" r="60" fill="#ffffffff"/>

  <!-- Enlarged play triangle (~5% bigger) -->
  <!-- Adjusted the triangle points outward slightly -->
  <polygon points="48,40 48,80 88,60" fill="#000000"/>
</svg>
